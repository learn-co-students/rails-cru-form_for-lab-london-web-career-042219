<div class="container">
  <br>
  <h1 class="text-danger">Genres: </h1>
  <br>
  <% @genres.each do |genre| %>
  <p class="text-danger"><%= link_to genre.name, genre_path(genre) %></p>
  <% end %>
  <br>
  <button type="button" class="btn btn-warning"><%= link_to "Main Menu", artists_path %></button>
</div>
